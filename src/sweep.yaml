method: bayes
metric:
  name: val_loss
  goal: minimize
parameters:
  model.lr:
    min: 1e-5
    max: 1e-2
    distribution: log_uniform  # Use log_uniform distribution for learning rate
  train.batch_size:
    values: [16, 32, 64]
  train.epochs:
    values: [10, 20, 30]